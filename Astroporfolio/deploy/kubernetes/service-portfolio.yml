apiVersion: v1
kind: Service
metadata:
  name: service-astroporfolio
  namespace: namespace-astroporfolio # Cambia esto
spec:
  type: NodePort
  selector:
    app: astroporfolio
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30086